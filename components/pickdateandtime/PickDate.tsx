import React from "react";

type Props = {};

const PickDate = (props: Props) => {
  return (
    <div>
      <div>
        <h1>Pick date & time </h1>
        <select>
          <option> </option>
        </select>
      </div>
    </div>
  );
};

export default PickDate;
